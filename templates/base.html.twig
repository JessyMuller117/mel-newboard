<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<link
		rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
			<link rel="stylesheet" href="style.css">
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body>
		{# {% for user in users  %}
			{% if user.collaborateurneweb == true %}
				{% include "/partials/_menu.html.twig" %}
			{% endif %}
		{% endfor %} #}

		
		{# <nav class="navbar navbar-expand-lg navbar-light bg-light">
					<div class="container-fluid">
						<a class="navbar-brand" href="#">N3WEB</a>
						<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
							<span class="navbar-toggler-icon"></span>
						</button>
						<div class="collapse navbar-collapse" id="navbarNav">
							<ul class="navbar-nav">
								<li class="nav-item">
		
									<a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="https://n3web.fr">N3web le site</a>
								</li>
		
		
							</ul>
							<ul class="navbar-nav">
								{% if app.user %}
									<li class="nav-item dropdown">
										<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownEntreprise" role="button" data-bs-toggle="dropdown" aria-expanded="false">
											N3wBoard
										</a>
										<ul class="dropdown-menu" aria-labelledby="navbarDropdownEntreprise">
											{% if app.user and app.user.collaborateurneweb == true %}
												<li class="nav-item">
													<a class="nav-link" href="{{path('app_account_n3web')}}">Liste des collaborateurs</a>
												</li>
												<li class="nav-item">
													<a class="nav-link" href="{{path('app_account_client')}}">Liste des clients</a>
												</li>
												<li class="nav-item">
													<a class="nav-link" href="{{path('app_entreprise_index')}}">Liste des entreprises</a>
												</li>
												{% elseif app.user == null %}
		
											{% endif %}
		
		
											{% if app.user %}
												{% for role in app.user.roles %}
													{% if app.user and role == 'ROLE_ADMIN' %}
														<li class="nav-item">
															<a class="nav-link" href="{{path('app_account_user')}}">Tous les utilisateurs</a>
														</li>
		
													{% endif %}
													{% if app.user and role == 'ROLE_CM' %}
														<li class="nav-item">
															<a class="nav-link" href="{{path('app_message_cm_index')}}">Tous les messages</a>
														</li>
													{% endif %}
												{% endfor %}
											{% endif %}
											{% if app.user and app.user.collaborateurneweb == false %}
												<li class="nav-item">
													<a class="nav-link" href="{{path('app_message_cm_new')}}">Envoyer des images</a>
												</li>
		
											{% endif %}
										</ul>
									</li>
		
								</li>
								{% else %}
		
							{% endif %}
						</ul>
						<ul class="navbar-nav">
							{% if app.user %}
								<li class="nav-item dropdown">
									<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
										{{app.user.prenom}}
									</a>
									<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
										{% if app.user %}
											{% for role in app.user.roles %}
												{% if app.user and role == 'ROLE_ADMIN' and 'ROLE_COMMERCIAL' %}
													<li>
														<a href="{{ path('app_entreprise_new')}}" class="dropdown-item">Ajouter une entreprise</a>
													</li>
		
												{% endif %}
											{% endfor %}
										{% endif %}
		
										<li>
											<a href="{{ path('app_account_index') }}" class="dropdown-item">Mon Compte</a>
										</li>
										<li>
											<a href="{{ path('app_account_profile')}}" class="dropdown-item">Modifier mon profil</a>
										</li>
										<li><hr class="dropdown-divider"></li>
										<li>
											<a class="dropdown-item" href="{{ path('logout')}}">Déconnexion</a>
										</li>
									</ul>
								</li>
		
							</li>
						{% else %}
							<li class="nav-item">
								<a href="{{ path('login') }}" class="nav-link">Connexion</a>
							</li>
						{% endif %}
					</ul>
				</div> #}
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
		{% block body %}{% endblock %}
	</body>
</html>
