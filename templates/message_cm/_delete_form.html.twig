<form method="post" action="{{ path('app_message_cm_delete', {'id': message_cm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message_cm.id) }}">
    <button class="btn btn-danger">Supprimer le message <img src="https://img.icons8.com/external-justicon-lineal-justicon/18/000000/external-bin-hotel-essentials-justicon-lineal-justicon.png"/></button>
</form>
